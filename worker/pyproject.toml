[project]
name = "swing-detector"
version = "0.1.0"
description = "Golf swing analysis backend with ML-powered pose estimation"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
